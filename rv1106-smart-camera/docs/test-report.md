# 测试报告

下表仅记录已经取得板端日志或客户端拉流结果的项目；未完成项不能据此推导性能或稳定性结论。

| 项目 | 状态 | 证据/说明 |
| --- | --- | --- |
| SC3336 controls 查询 | 已验证 | 阶段 1 基线：exposure、analogue gain、vblank、hflip、vflip、test pattern 可见 |
| 主采集单帧与 stream on/off | 已验证 | `/dev/video11`，2304×1296 NV12 单帧抓取成功 |
| daemon 启动与 Unix Socket | 已验证 | 2021-01-01 板端时钟：Socket `get_status` 返回 NORMAL、失败计数 0 |
| 既有 sender/RGA/预览链路 | 已验证 | sender 日志记录 720P/360P RTSP、384×216 RGA 预览，RGA 错误 0 |
| RKNN 本地检测 | 已验证 | NPU 延迟启动后 PID 有效；日志记录连续推理（约 16.6–19.9 ms） |
| Qt 全屏控制台/LCD 预览 | 已验证 | 2021-01-01：可恢复接管测试运行 8 秒，DMA-BUF 预览约 15 FPS，Qt 保持存活 |
| Qt 触摸交互 | 已验证 | 已在 LCD 上进入设备调试页、切换页面并操作参数控件 |
| 手动曝光/增益 | 已验证 | 自动 AE 关闭后曝光设为 320，3 秒后板端回读仍为 320；随后恢复 128/128 安全基线 |
| 连续手动参数与返回预览 | 已验证 | 2026-08-20：连续设置曝光 320、增益 256 后回读保持；返回预览未重启 `media-sender`（PID 保持 2925），Qt 帧号持续增长约 15 FPS；最后恢复 128/128 安全基线 |
| 翻转/测试图 | 待验证 | 通过标准 V4L2 control 验证实际画面 |
| 低照度切换 | 待验证 | 需接入真实亮度指标生产端，不以模拟数据宣称完成 |
| 720P/360P RTSP 播放端验证 | 已验证 | 2026-08-20：板端 FFmpeg 通过 TCP 拉取 8554 `/live/0`（1280×720，3 秒 75 帧）和 `/live/1`（640×360，3 秒 61 帧），两次均返回 0；拉流后 Qt 预览继续保持约 15 FPS |
| 流异常恢复 | 待验证 | 读取 `events.jsonl`，确认受限重建与 ERROR 路径 |
| 连续启停请求接口 | 已验证 | `stress_test.sh 2` 两轮 Socket 请求均返回 ok，并保留日志 |
| 连续启停完成性 | 待验证 | sender 信号退出/重建的完整事件需在独立板端会话继续确认；本轮不宣称完成 |
| 长稳测试 | 待验证 | 记录时间、温度、错误与资源占用后再给结论 |

## 字体修复记录

已从板端既有 UI 复用 `DroidSansFallbackFull.ttf`，并由部署脚本安装到
`/userdata/rv1106-smart-camera/bin/fonts/`。2021-01-01 的 5 秒接管复测日志显示
`#Font loaded: Droid Sans Fallback`；中文字体加载问题已修复。日志文件为追加式，
其中较早的失败行属于修复前测试，不能与本次成功结果混淆。
